import { RoledisplayPipe } from './roledisplay.pipe';

describe('RoledisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new RoledisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
